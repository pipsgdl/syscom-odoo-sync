# =============================================================================
# Ingram Micro — OAuth2 (refresh token rotativo desde browser)
# =============================================================================
#
# 1. Loguea en https://mx.ingrammicro.com con rz80 / Tunel$2380.
# 2. Abre DevTools (F12) → tab Console
# 3. Pega este snippet de UNA línea (todo, sin saltos):
#
#    var o = JSON.parse(localStorage['okta-token-storage']); copy(`INGRAM_REFRESH_TOKEN=${o.refreshToken.refreshToken}\nINGRAM_OAUTH_CLIENT_ID=${o.idToken.clientId}\nINGRAM_CUSTOMER_NUMBER=80697300`); console.log('COPIADO al clipboard. Pega abajo.');
#
# 4. Pega el contenido del clipboard reemplazando las 3 líneas siguientes:

INGRAM_REFRESH_TOKEN=
INGRAM_OAUTH_CLIENT_ID=
INGRAM_CUSTOMER_NUMBER=80697300

# =============================================================================
# Banxico (opcional, si lo registras en sieapis.banxico.org.mx)
# =============================================================================
# Si está vacío, el script usa fallback open.er-api.com (sin auth)
BANXICO_TOKEN=

# =============================================================================
# Excel del Norte (pendiente — Felipe debe llamar al 800-700-3935)
# =============================================================================
EXEL_API_KEY=
